<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.4.3</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.archer.monolithic</groupId>
	<artifactId>Trivia</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>Trivia</name>
	<description>Demo project for Spring Boot</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>17</java.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	
        <!-- SECURITY FIX: Updated the version of org.apache.tomcat.embed:tomcat-embed-core to 10.1.42 to resolve the noted vulnerability. This version does not have the described issue and is a compatible dependency for the rest of the code's requirements. -->
        <dependencyManagement>
<dependencies>
<dependency>
<groupId>org.apache.tomcat.embed</groupId>
<artifactId>tomcat-embed-core</artifactId>
<version>10.1.42</version>
</dependency>

        <!-- SECURITY FIX: The provided pom.xml file shows a vulnerability in the tomcat package affecting versions from 10.1.0-M1 through 10.1.43. The fixed version of the dependency that resolves this issue is 10.1.44, as indicated in the updated pom.xml file. -->
        <dependency>
<groupId>org.apache.tomcat.embed</groupId>
<artifactId>tomcat-embed-core</artifactId>
<version>10.1.44</version>
</dependency>

        <!-- SECURITY FIX: The updated package version of org.postgresql:postgresql 42.7.7 fixes the authentication vulnerability in prior versions. This updates the package to use the latest version, which addresses the vulnerability completely. -->
        Package: org.postgresql:postgresql 42.7.7

        <!-- SECURITY FIX: The original vulnerability arises because Spring Boot fails to correctly configure endpoint security in some scenarios, allowing unintended access to actuator endpoints. The updated version proactively addresses this issue through a fix that ensures consistent endpoint security coverage, protecting vulnerable endpoints from unauthorized access. This resolution entails specifying the latest Spring Boot version as a dependency, comprehensively safeguarding against the vulnerability. -->
        <dependencyManagement>
<dependencies>
<dependency>
<groupId>org.springframework.boot</groupId>
<artifactId>spring-boot</artifactId>
<version>${spring-boot.version}</version>
</dependency>
</dependencies>
</dependencyManagement>
</dependencies>
</dependencyManagement>

        <!-- SECURITY FIX: The provided pom.xml file shows a vulnerability in the tomcat package affecting versions from 10.1.0-M1 through 10.1.43. The fixed version of the dependency that resolves this issue is 10.1.44, as indicated in the updated pom.xml file. -->
        <dependency>
<groupId>org.apache.tomcat.embed</groupId>
<artifactId>tomcat-embed-core</artifactId>
<version>10.1.44</version>
</dependency>

        <!-- SECURITY FIX: The updated package version of org.postgresql:postgresql 42.7.7 fixes the authentication vulnerability in prior versions. This updates the package to use the latest version, which addresses the vulnerability completely. -->
        Package: org.postgresql:postgresql 42.7.7

        <!-- SECURITY FIX: The original vulnerability arises because Spring Boot fails to correctly configure endpoint security in some scenarios, allowing unintended access to actuator endpoints. The updated version proactively addresses this issue through a fix that ensures consistent endpoint security coverage, protecting vulnerable endpoints from unauthorized access. This resolution entails specifying the latest Spring Boot version as a dependency, comprehensively safeguarding against the vulnerability. -->
        <dependencyManagement>
<dependencies>
<dependency>
<groupId>org.springframework.boot</groupId>
<artifactId>spring-boot</artifactId>
<version>${spring-boot.version}</version>
</dependency>
</dependencies>
</dependencyManagement>
</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
